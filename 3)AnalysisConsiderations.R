#Analysis considerations:
# Descriptive - any(is.na()), dim(), head(), str() & summary(), mean(), median(), mode()
#                 max(), min(), range(), length(), class(), names()
#
# Exploratory - pairs(), histograms(), boxplots(), barplot() QQ plots & multiple plots
#
# Analysis - OLS, SLR, BiVariate Regression, Multivariate Linear Regression, Heatmaps,
#                 HCL, PCA, SVD, Mean, T-Test, Z-Test, covariance, OLS, regression to mean (-1),
#                 simple linear regression, statistical linear regression, multivariable
#                 regression & model selection, adjustments, residuals (predict fit, residual
#                 fit (-1)), hatvalues, variation, & dfbetas, R^2, diagnostics; ANOVA, GLMs &
#                 Binary GLMs, coeficients, correlation, confidence intervals, Cooks Distance,
#                 ChiSq-Test, VIF, binary, binomial, poisson, intercept, slope, influence &
#                 leverage, Odds & OddRatio, Predictive, Causal ~ NA, Mechanistic ~ NA
