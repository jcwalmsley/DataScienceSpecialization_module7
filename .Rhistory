```
---
# Chunk 3: Data overview
rm(list = ls()); data("mtcars"); any(is.na(mtcars)); head(mtcars,5)
```
---
# Chunk 4: Process data
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V", "S")); mtcars$am <- factor(mtcars$am, labels = c("A", "M")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
```
---
# Chunk 5
pairs(mpg ~ ., data = mtcars)
```
---
Histograms
# Chunk 6
data("mtcars"); require(ggplot2); require(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
```
---
Box Plots
# Chunk 7
boxplot(mpg ~ am, data = mtcars, col = (c("grey","grey")), ylab = "Miles-Per-Gallon", xlab = "Transmission", main =  "Boxplot MPG")
```
---
QQ Plots
# Chunk 8
#qqPlot(fit?, main="Q-Q plot")
```
---
Fitted
# Chunk 9
```
---
Residuals
# Chunk 10
#plot(resid(fit?))
#abline(h=0)
```
---
Residuals vs Fitted
# Chunk 11
# To be decided
```
---
pairs(mpg ~ ., data = mtcars)
```
---
Histograms
pairs(mpg ~ ., data = mtcars)
```
---
Histograms
# Chunk 1
sessionInfo()
```
---
# Chunk 2: Get data
library(datasets)
data("mtcars")
head(mtcars)
```
---
# Chunk 3: Data overview
rm(list = ls()); data("mtcars"); any(is.na(mtcars)); head(mtcars,5)
```
---
# Chunk 4: Process data
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V", "S")); mtcars$am <- factor(mtcars$am, labels = c("A", "M")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
```
---
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V", "S")); mtcars$am <- factor(mtcars$am, labels = c("A", "M")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
```
---
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("Vblock", "Sblock")); mtcars$am <- factor(mtcars$am, labels = c("Atype", "Mtype")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
```
---
setwd("~/Desktop/Coursera_R/7_Regression Models/RM_proj_MPG_MotorTrendData")
# Chunk 1
sessionInfo()
```
---
# Chunk 2: Get data
library(datasets)
data("mtcars")
head(mtcars)
```
---
# Chunk 3: Data overview
rm(list = ls()); data("mtcars"); any(is.na(mtcars)); head(mtcars,5)
```
---
# Chunk 4
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
# Chunk 5
pairs(mpg ~ ., data = mtcars)
# Chunk 6
data("mtcars"); require(ggplot2); require(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
# Chunk 7
boxplot(mpg ~ am, data = mtcars, col = (c("grey","grey")), ylab = "Miles-Per-Gallon", xlab = "Transmission", main =  "Boxplot MPG")
# Chunk 8
#qqPlot(fit?, main="Q-Q plot")
# Chunk 9
# Chunk 10
#plot(resid(fit?))
#abline(h=0)
# Chunk 11
# To be decided
data("mtcars"); library(ggplot2); library(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
# Chunk 1
sessionInfo()
```
---
# Chunk 2: Get data
library(datasets)
data("mtcars")
head(mtcars)
```
---
# Chunk 3: Data overview
rm(list = ls()); data("mtcars"); any(is.na(mtcars)); head(mtcars,5)
```
---
# Chunk 4
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
# Chunk 5
pairs(mpg ~ ., data = mtcars)
# Chunk 6
data("mtcars"); library(ggplot2); library(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
# Chunk 7
boxplot(mpg ~ am, data = mtcars, col = (c("grey","grey")), ylab = "Miles-Per-Gallon", xlab = "Transmission", main =  "Boxplot MPG")
# Chunk 8
#qqPlot(fit?, main="Q-Q plot")
# Chunk 9
# Chunk 10
#plot(resid(fit?))
#abline(h=0)
# Chunk 11
# To be decided
mtcars
library(datasets)
head(mtcars)
```
---
library(datasets)
head(mtcars)
```
---
rm(list = ls()); data("mtcars"); any(is.na(mtcars)); head(mtcars,5)
```
---
rm(list = ls());
library(datasets)
head(mtcars)
```
---
rm(list = ls());library(datasets);head(mtcars)
```
---
rm(list = ls());sessionInfo()
```
---
sessionInfo()
```
---
sessionInfo()
```
---
sessionInfo()
```
---
sessionInfo()
```
---
sessionInfo()
```
---
library(datasets);head(mtcars)
```
---
data("mtcars"); any(is.na(mtcars)); head(mtcars,5)
```
---
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
any(is.na(mtcars)); head(mtcars,5)
```
---
head(mtcars)
```
---
rm(list = ls());library(UsingR); head(mtcars)
```
---
rm(list = ls());library(UsingR); head("mtcars"")
```
---
rm(list = ls());library(datsets); head("mtcars"")
sessionInfo()
```
---
rm(list = ls());library(datsets); head("mtcars"")
```
---
head("mtcars"")
```
---
head("mtcars")
```
---
rm(list=ls());library(datasets);head("mtcars")
```
---
rm(list=ls()); library(datasets); head("mtcars")
```
---
# Chunk 1: Session Info
sessionInfo()
```
---
# Chunk 2: Get data
rm(list=ls()); library(datasets); head("mtcars")
```
---
# Chunk 3: Data overview
any(is.na(mtcars)); head(mtcars,5)
```
---
# Chunk 4: Process data
data("mtcars")
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
# Chunk 5: Pairs plot
pairs(mpg ~ ., data = mtcars)
# Chunk 6: Hist plot
data("mtcars"); library(ggplot2); library(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
# Chunk 7: Box plot
boxplot(mpg ~ am, data = mtcars, col = (c("grey","grey")), ylab = "Miles-Per-Gallon", xlab = "Transmission", main =  "Boxplot MPG")
# Chunk 8: QQ plot
#qqPlot(fit?, main="Q-Q plot")
# Chunk 9: Fitted plot
# Chunk 10: Residual plot
#plot(resid(fit?))
#abline(h=0)
# Chunk 11: Residuals vs Fitted plot
# To be decided
data(mtcars)
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
rm(list=ls()); library(datasets); head(mtcars)
```
---
rm(list=ls()); library(datasets); head(mtcars)
```
---
# Chunk 1: Session Info
sessionInfo()
```
---
library(datasets); head(mtcars)
```
---
library(datasets);head(mtcars)
```
---
head(mtcars)
```
---
library(UsingR)
library(datasets)
head(mtcars)
library(UsingR)
library(datasets)
head(mtcars)
```
---
rn(list=ls())
rm(list=ls())
library(UsingR)
library(datasets)
head(mtcars)
rm(list=ls())
library(UsingR)
library(datasets)
head(mtcars)
```
---
rm(list=ls());library(UsingR);library(datasets);head(mtcars)
```
---
rm(list=ls());library(UsingR);library(datasets);head(mtcars)
```
---
rm(list=ls()); library(UsingR); library(datasets); head(mtcars)
```
---
rm(list=ls()); library(UsingR); library(datasets); head(mtcars)
```
---
# Chunk 1: Session Info
sessionInfo()
```
---
# Chunk 2: Get data
rm(list=ls()); library(UsingR); library(datasets); head(mtcars)
```
---
# Chunk 3: Data overview
any(is.na(mtcars)); head(mtcars,5)
```
---
# Chunk 4: Process data
data(mtcars)
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb)
str(mtcars); head(mtcars)
# Chunk 5: Pairs plot
pairs(mpg ~ ., data = mtcars)
# Chunk 6: Hist plot
data("mtcars"); library(ggplot2); library(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
# Chunk 7: Box plot
boxplot(mpg ~ am, data = mtcars, col = (c("grey","grey")), ylab = "Miles-Per-Gallon", xlab = "Transmission", main =  "Boxplot MPG")
# Chunk 8: QQ plot
#qqPlot(fit?, main="Q-Q plot")
# Chunk 9: Fitted plot
# Chunk 10: Residual plot
#plot(resid(fit?))
#abline(h=0)
# Chunk 11: Residuals vs Fitted plot
# To be decided
data("mtcars"); library(ggplot2); library(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
any(is.na(mtcars)); head(rownames(mtcars),5)
```
---
any(is.na(mtcars)); head(rownames(mtcars))
```
---
any(is.na(mtcars)); rownames(mtcars)
```
---
any(is.na(mtcars)); colnames(mtcars)
```
---
rm(list=ls()); data("mtcars")
```
---
# Chunk 1: Session Info
sessionInfo()
```
---
rm(list=ls()); data("mtcars")
```
---
library(UsingR)
data("mtcars")
head(mtcars)
library(UsingR)
data("mtcars")
head(mtcars)
rm(list=ls()); data("mtcars")
```
---
any(is.na(mtcars)); colnames(mtcars)
```
---
data(mtcars)
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb); head(mtcars)
# Chunk 1: Session Info
sessionInfo()
```
---
# Chunk 2: Get data
rm(list=ls()); data("mtcars")
```
---
# Chunk 3: Data overview
any(is.na(mtcars)); colnames(mtcars)
```
---
# Chunk 4: Process data
data(mtcars)
mtcars$vs <- factor(mtcars$vs, labels = c("V-block", "S-block")); mtcars$am <- factor(mtcars$am, labels = c("A-type", "M-type")); mtcars$gear <- factor(mtcars$gear); mtcars$carb <- factor(mtcars$carb); head(mtcars)
# Chunk 5: Pairs plot
pairs(mpg ~ ., data = mtcars)
# Chunk 6: Hist plot
data("mtcars"); library(ggplot2); library(dplyr);
hist(mtcars$mpg, breaks=15, xlab = "Miles-Per-gallon", main = "MPG Histogram", xlim = range(5:40))
# Chunk 7: Box plot
boxplot(mpg ~ am, data = mtcars, col = (c("grey","grey")), ylab = "Miles-Per-Gallon", xlab = "Transmission", main =  "Boxplot MPG")
# Chunk 8: QQ plot
#qqPlot(fit?, main="Q-Q plot")
# Chunk 9: Fitted plot
#To be inserted
# Chunk 10: Residual plot
#plot(resid(fit?))
#abline(h=0)
# Chunk 11: Residuals vs Fitted plot
# To be inserted
install.packages("Swirl")
install.packages("swirl")
swirl()
library(swirl)
swirl()
rpg1
rgp1 <- function(){
print("Processing. Please wait.")
# number of samples per simulation
n <- 100
# number of simulations
nosim <- 1000
# set seed for reproducibility
set.seed(4321)
# Point A
x1 <- rnorm(n)
x2 <- rnorm(n)
x3 <- rnorm(n)
# Point B
betas <- sapply(1 : nosim, function(i)makelms(x1, x2, x3))
round(apply(betas, 1, var), 5)
}
>
rgp1 <- function(){
print("Processing. Please wait.")
# number of samples per simulation
n <- 100
# number of simulations
nosim <- 1000
# set seed for reproducibility
set.seed(4321)
# Point A
x1 <- rnorm(n)
x2 <- rnorm(n)
x3 <- rnorm(n)
# Point B
betas <- sapply(1 : nosim, function(i)makelms(x1, x2, x3))
round(apply(betas, 1, var), 5)
}
rpg1()
rgp1()
rgp2()
head(swiss)
mdl <- lm(Fertility ~ ., data = swiss)
vif(mdl)
mdl2 <- lm(Fertility ~ . - Examination, data = swiss)
vif(mdl2)
x1c <- simbias()
apply(x1c,1,mean)
fit1 <- lm(Fertility ~ Agriculture, data = swiss)
fit3 <- lm(Fertlity, Agriculture + Examination + Education, data = swiss)
fit3 <- lm(Fertility, Agriculture + Examination + Education, data = swiss)
fit3 <- lm(Fertility ~ Agriculture + Examination + Education, data = swiss)
anova(fit1, fit3)
deviance(fit3)
d <- deviance(fit3)/43
n <- deviance(fit1)-deviance(fit3)
n <- (deviance(fit1)-deviance(fit3))/2
n/d
pf(n/d, 2, 43, lower.tail = FALSE)
shapiro.test(fit3$residuals)
anova(fit1,fit3,fit5,fit6)
ravenData
mdl <- glm(ravenWinNum ~ ravenScore, family = binomial, ravenData)
lodds <- predict(mdl, data.frame(ravenScore=c(0,3,6)))
exp(lodds)/(1+exp(lodds))
summarise(mdl)
summary(mdl)
confint(mdl)
exp(confint(mdl))
anova
anova(mdl)
qchisq(0.95,1)
var(rpois(1000, 50)
>
var(rpois(1000, 50))
nxt()
head(hits)
class(hits[,'date'])
as.integer(head(hits[,'date']))
mdl <- glm(visits ~ date, family = poisson, hits)
summary(mdl)
exp(confint(mdl))
exp(confint(mdl, 'date')); exp(confint(mdl,2))
which.max(hits[,'visits'])
hits[704,]
lambda <- mdl$fitted.values[704]
qpois(.95, lambda)
mdl2 <- glm(log(visits+1))
mdl2 <- offset=log(visits+1))
mdl2 <- glm(offset=log(visits+1))
mdl2 <- offset=log(visits+1)
mdl2 <- log(visits+1)
mdl <- glm(visits+1 ~ date, family = poisson, hits)
mdl2 <- glm(formula = simplystats ~ date, family = poisson, data = hits, offset = log(visits + 1))
qpois(.95,mdl2$fitted.values[704])
